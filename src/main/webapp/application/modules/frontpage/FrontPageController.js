(function(angular, SVG, gapi, $) {
    'use strict';

    angular.module('svb.frontpage', [])
        .controller('FrontPageController', ['$scope', '$log', function($scope, $log) {
        }]);
}(window.angular, window.SVG, window.gapi, window.jQuery));
